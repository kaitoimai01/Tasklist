<h1><%= @user.name %>の詳細ページ</h1>
  
  <table>
   <tr>
    <th>名前</th><td><%= @user.name %></td>
   </tr>

   <tr>
    <th>メールアドレス</th><td><%= @user.email %></td>
   </tr>

   <tr>
    <th>管理者権限</th><td><%= @user.admin %></td>
   </tr>

   <tr>
    <th>登録日時</th><td><%= @user.created_at %></td>
   </tr>

   <tr>
    <th>更新日時</th><td><%= @user.updated_at %></td>
   </tr>
  </table>

<%# <%= link_to '編集', edit_admin_user_path(user), class: "btn btn-primary" %>
<%# <%= link_to '削除', [:admin, user], method: :delete, data: { confirm: "ユーザー「#{user.name}」を削除します。よろしいですか？"}, class: 'btn btn-danger btn-xs' %>