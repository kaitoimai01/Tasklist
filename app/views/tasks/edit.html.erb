<h1>id: <%= @task.id%> のタスク編集ページ</h1>

  <%= form_for(@task) do |f| %>
   <%= f.label :title, 'タイトル' %>
   <%= f.text_field :title %>

   <%= f.label :content, '内容' %>
   <%= f.text_field :content %>
   
   <%= f.submit '投稿' %>
  <% end %>