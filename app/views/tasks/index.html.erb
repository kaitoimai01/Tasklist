<h1>検索フォーム</h1>
 <%= render 'search' %>

<h1>タスク一覧</h1>
 <ul>
   <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : <%= task.status %> : <%= task.title %> &gt; <%= task.content %> :　✔︎ <%= task.deadline %></li>
   <% end %>
 </ul>

 <%= link_to '新規タスクの追加', new_task_path %>