<h1>タスク新規作成ページ</h1>

  <%= form_for(@task) do |f| %>
   <%= f.label :title, 'タイトル' %>
   <%= f.text_field :title %>

   <%= f.label :content, '内容' %>
   <%= f.text_field :content %>
   
   <%= f.submit '投稿' %>
  <% end %>