<h1>タスク一覧</h1>
 <ul>
  <% @tasks.each do |task| %> 
  <li>
   <%= task.title %>
   <%= task.content %>
   <%= link_to task.id, task %>
  </li>
  <% end %>
 </ul>

 <%= link_to '新規タスクの追加', new_task_path %>